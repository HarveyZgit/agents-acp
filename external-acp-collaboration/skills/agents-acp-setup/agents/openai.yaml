interface:
  display_name: "Agents ACP setup"
  short_description: "Initialize or change the default ACP agent and model"
  default_prompt: "Initialize or update agents-acp defaults for this workspace. Ask which agent (cursor or grok) and optional model to store, then persist them."

dependencies:
  tools:
    - type: "mcp"
      value: "agents-acp"
      description: "Bundled agents-acp MCP server (get_config, configure)."
