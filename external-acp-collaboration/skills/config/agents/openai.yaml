interface:
  display_name: "Agents ACP config"
  short_description: "Initialize or change the default ACP agent and model"
  default_prompt: "Configure agents-acp defaults for this workspace. Ask which agent (cursor, grok, or antigravity) and optional model to store, then persist them."

dependencies:
  tools:
    - type: "mcp"
      value: "agents-acp"
      description: "Bundled agents-acp MCP server (get_config, configure)."
